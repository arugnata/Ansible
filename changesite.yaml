- name: Change default Nginx site
  hosts: ec2_nodes
  become: true

  tasks:
    - name: Create custom index page
      copy:
        dest: /var/www/html/index.html
        content: |
          <h1>Welcome to My Ansible Managed EC2 Server</h1>
          <p>Configured using Ansible Playbook</p>
